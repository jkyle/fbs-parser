import createGame from './game';
import processor from './processor';
import defaultMiddleware from './middleware';
import { look, take, go, nothing, clear, start } from './middleware';

export { createGame, processor, defaultMiddleware,
         look, take, go, nothing, clear, start }